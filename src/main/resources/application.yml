spring:
  profiles:
    active:
      - dev
  datasource:
    username: root
    password: password
    url: jdbc:mysql://127.0.0.1:root/arkpool?usessl=true&characterEncoding=utf-8&useUnicode=true
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database: mysql
    hibernate:
      ddl-auto: update
